# =============================================
# Service cho Tb-core
# =============================================
apiVersion: v1
kind: Service
metadata:
  name: tb-core
  namespace: thingsboard
spec:
  selector:
    app: tb-core
  ports:
    - name: tb-core
      port: 8080
      targetPort: 8080

---
# =============================================
# Deployment cho Tb-core (docker.io/thingsboard/tb-node:3.6.0)
# =============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tb-core
  namespace: thingsboard
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tb-core
  template:
    metadata:
      labels:
        app: tb-core
    spec:
      containers:
        - name: tb-core
          image: docker.io/thingsboard/tb-node:3.6.0
          ports:
            - containerPort: 8080
            - containerPort: 7070
          resources:
            requests:
              cpu: "2"
              memory: "2Gi"
            limits:
              cpu: "4"
              memory: "4Gi"
          volumeMounts:
            - name: tb-conf-volume
              mountPath: /config
          env:
            - name: TB_SERVICE_TYPE
              value: "tb-core"
            - name: EDGES_ENABLED
              value: "true"
            - name: ZOOKEEPER_ENABLED
              value: "true"
            - name: ZOOKEEPER_URL
              value: "zookeeper:2181"
            - name: JS_EVALUATOR
              value: "remote"
            - name: TRANSPORT_TYPE
              value: "remote"
            - name: HTTP_LOG_CONTROLLER_ERROR_STACK_TRACE
              value: "false"
            - name: METRICS_ENABLED
              value: "true"
            - name: METRICS_ENDPOINTS_EXPOSE
              value: "prometheus"
            #DATABASE
            - name: DATABASE_TS_TYPE
              value: "cassandra"
            - name: CASSANDRA_URL
              value: "172.18.0.1:9042"
            - name: SPRING_DRIVER_CLASS_NAME
              value: "org.postgresql.Driver"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://172.18.0.1:5432/thingsboard"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "postgres"
            #QUEUE
            - name: TB_QUEUE_TYPE
              value: "kafka"
            - name: TB_KAFKA_SERVERS
              value: "kafka:9092"
              #CACHE
            - name: CACHE_TYPE
              value: "redis"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
      volumes:
        - name: tb-conf-volume
          configMap:
            name: tb-core-conf
            items:
              - key: "logback.xml"
                path: "logback.xml"
              - key: "thingsboard.conf"
                path: "thingsboard.conf"
